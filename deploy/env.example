# Local .env for deploy (gitignored). Copy to hotel-search/.env
# Used by serverless-dotenv-plugin + bundled in Lambda zip (authorizer layer reads these).

GUEST_JWT_SECRET=alrais-guest-token
GUEST_TOKEN_TTL=19800
COGNITO_USERPOOL_ID=eu-west-1_rSwSQrvsW
COGNITO_REGION=eu-west-1
COGNITO_CLIENT_ID=4ll37dkksjgjb65cp96fc70k71
DDB_RATE_TABLE=AuthMiddlewareRateTable

# If hotelSearch internal routing uses these, copy from hotel-search-dev-hotelDetail in AWS:
# INTERNAL_SUPPLIER_ROUTING_KEY=
# INTERNAL_BASE_URL=

# GIATA: stored in SSM (/provesio/GIATA_*) — injected at deploy via serverless-ssm-fetch, not needed here.

# Google Places (reviews). Do not reuse Provisio X_API_KEY.
GOOGLE_PLACES_API_KEY=
